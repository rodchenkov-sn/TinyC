name: Test
on: [push]
jobs:
  build:
    name: Build
    runs-on: self-hosted
    steps:
      - uses: actions/checkout@v2
      - name: Make build dir
        run: mkdir build && cd build
      - name: Cmake
        run: cmake ..
      - name: Build
        run: make
