name: Test
on: [push]
jobs:
  build:
    name: Build
    runs-on: self-hosted
    steps:
      - uses: actions/checkout@v2
      - run: mkdir build
      - name: Cmake
        working-directory: ./build
        run: cmake ..
      - name: Build
        working-directory: ./build
        run: make
